{
  "name": "sonner-lite-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bun run --filter @lukeberrypi/sonner-lite build",
    "dev": "bun run --filter website dev",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^2.8.4"
  },
  "packageManager": "bun@1.3.8"
}
